services:
  - type: web
    name: signlanguage-backend
    env: python
    runtime: python-3.10
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    region: oregon  # hoặc oregon, tuỳ vị trí bạn chọn
