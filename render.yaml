services:
  - type: web
    name: signlanguage-backend
    env: python
    runtime: python-3.9
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"  # thay đổi nếu bạn dùng file main khác

